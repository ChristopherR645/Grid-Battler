using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class EnemyPlaceHolderMovement : MonoBehaviour
{
    //public EnemyMovement EnemyMovement;
   // public float time = 5;
    //public float timeStore;

    // Start is called before the first frame update
    void Start()
    {
        //timeStore = time;
    }

    // Update is called once per frame
    void Update()
    {/*
        if (time > 0) {

            time -= Time.deltaTime;
        }
        else
        {
            int rand = Random.Range(1, 100);
            if (rand > 50)
            {
                EnemyMovement.Up();
            }
            else
            {
                EnemyMovement.Down();
            }
            time = timeStore;
        }*/
    }
}
